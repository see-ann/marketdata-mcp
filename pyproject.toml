[project]
name = "marketdata-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.27.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
    "pre-commit>=4.6.0",
]

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.format]
quote-style = "double"